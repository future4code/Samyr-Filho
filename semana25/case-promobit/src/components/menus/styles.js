import styled from "styled-components"

export const GenersStyles = styled(theme)`
    button: {
        display: 'block';
        margin-top : theme.spacing(2)
      }
    formControl: {
        margin: theme.spacing(1);
        min-Width: 120
      }
`