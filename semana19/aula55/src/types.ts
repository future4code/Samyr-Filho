export interface authenticationData{
    id: string
  }

export interface user  {
    id: string
    email: string
    password:string
 }
 export interface userInfo {
     id: string
     email: string
 }